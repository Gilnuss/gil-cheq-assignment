[project]
name = "churn-mcp"
version = "0.1.0"
description = "MCP server for natural-language analytics over the Telco Customer Churn dataset"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "duckdb>=1.1.0",
]
